\DTLgidxSetDefaultDB{notation}

\newterm
[% 
label={not:xt}, 
description={current raw sensor value at time \ensuremath{t}}, 
sort={xt} 
]%
{\ensuremath{x_t}}

\newterm
[% 
label={not:xtsmooth}, 
description={smoothed sensor value at time \ensuremath{t} after applying moving average}, 
sort={xtsmooth} 
]%
{\ensuremath{x^{\text{smooth}}_t}}

\newterm
[%
label={not:N},
description={window size for the moving average filter},
sort={N}
]%
{\ensuremath{N}}

\newterm
[%
label={not:xtmed},
description={median-filtered sensor value at time \ensuremath{t}},
sort={xtmed}
]%
{\ensuremath{x^{\text{med}}_t}}

\newterm
[%
label={not:W},
description={sliding window length of the median filter},
sort={W}
]%
{\ensuremath{W}}

\newterm
[%
label={not:yt},
description={exponential moving average output at time \ensuremath{t}},
sort={yt}
]%
{\ensuremath{y_t}}

\newterm
[%
label={not:alpha},
description={smoothing factor of the exponential moving average},
sort={alpha}
]%
{\ensuremath{\alpha}}

\newterm
[%
label={not:tau},
description={time constant of the exponential moving average},
sort={tau}
]%
{\ensuremath{\tau}}

\newterm
[%
label={not:delta},
description={dead-band publishing threshold},
sort={delta}
]%
{\ensuremath{\delta}}

\newterm
[%
label={not:ypub},
description={last published dead-band fuel value},
sort={ypub}
]%
{\ensuremath{y_{\text{pub}}}}

\newterm
[% 
label={not:Vsensori}, 
description={fuel volume estimated by the sensor at sample \ensuremath{i}}, 
sort={Vsensori} 
]%
{\ensuremath{V_{\text{sensor},i}}}

\newterm
[% 
label={not:Vmanuali}, 
description={actual (manual) fuel volume measurement at sample \ensuremath{i}}, 
sort={Vmanuali} 
]%
{\ensuremath{V_{\text{manual},i}}}

\newterm
[% 
label={not:Tsent}, 
description={timestamp when a data packet is sent}, 
sort={Tsent} 
]%
{\ensuremath{T_{\text{sent}}}}

\newterm
[% 
label={not:Treceived}, 
description={timestamp when a data packet is received}, 
sort={Treceived} 
]%
{\ensuremath{T_{\text{received}}}}

\newterm
[% 
label={not:L}, 
description={data transmission latency}, 
sort={L} 
]%
{\ensuremath{L}}

\newterm
[% 
label={not:Ti}, 
description={GPS timestamp at index \ensuremath{i}}, 
sort={Ti} 
]%
{\ensuremath{T_i}}

\newterm
[% 
label={not:ti}, 
description={time difference between successive GPS logs}, 
sort={ti} 
]%
{\ensuremath{t_i}}

\newterm
[% 
label={not:Tseries}, 
description={time series of fuel usage}, 
sort={T} 
]%
{\ensuremath{T}}

\newterm
[% 
label={not:m}, 
description={window length for subsequence in time series analysis}, 
sort={m} 
]%
{\ensuremath{m}}

\newterm
[% 
label={not:Pi}, 
description={matrix profile value at index \ensuremath{i}}, 
sort={Pi} 
]%
{\ensuremath{P[i]}}

\newterm
[% 
label={not:xvec}, 
description={input feature vector for anomaly detection}, 
sort={x} 
]%
{\ensuremath{\mathbf{x}}}

\newterm
[% 
label={not:sx}, 
description={anomaly score for vector \ensuremath{\mathbf{x}} from Isolation Forest}, 
sort={sx} 
]%
{\ensuremath{s(\mathbf{x})}}

\newterm
[% 
label={not:Ehx}, 
description={expected path length for \ensuremath{\mathbf{x}} in an Isolation Forest}, 
sort={Ehx} 
]%
{\ensuremath{E(h(\mathbf{x}))}}

\newterm
[% 
label={not:cn}, 
description={average path length for unsuccessful search in a BST with \ensuremath{n} points}, 
sort={cn} 
]%
{\ensuremath{c(n)}}

\newterm
[% 
label={not:TP}, 
description={true positive}, 
sort={TP} 
]%
{\ensuremath{TP}}

\newterm
[% 
label={not:FP}, 
description={false positive}, 
sort={FP} 
]%
{\ensuremath{FP}}

\newterm
[% 
label={not:TN}, 
description={true negative}, 
sort={TN} 
]%
{\ensuremath{TN}}

\newterm
[% 
label={not:FN}, 
description={false negative}, 
sort={FN} 
]%
{\ensuremath{FN}}

\newterm
[
 label={not:Tdetected},
 description={timestamp when an anomaly is detected},
 sort={Tdetected}
]%
{\ensuremath{T_{\text{detected}}}}

\newterm
[
 label={not:Tactual},
 description={timestamp of actual anomaly occurrence},
 sort={Tactual}
]%
{\ensuremath{T_{\text{actual}}}}

\newterm
[%
label={not:phat},
description={estimated binomial proportion (sample success rate)},
sort={phat}
]%
{\ensuremath{\hat{p}}}

\newterm
[%
label={not:z},
description={standard normal quantile used in the Wilson score interval},
sort={z}
]%
{\ensuremath{z}}

\newterm
[%
label={not:n},
description={number of samples or validation events},
sort={n}
]%
{\ensuremath{n}}

\newterm
[%
label={not:Ctank},
description={nominal tank capacity used to convert fuel volume to percentage},
sort={Ctank}
]%
{\ensuremath{C_{\text{tank}}}}

\newterm
[%
label={not:Vpumpi},
description={fuel volume dispensed by the pump during validation event \ensuremath{i}},
sort={Vpumpi}
]%
{\ensuremath{V_{\text{pump},i}}}

\newterm
[%
label={not:VHMIi},
description={fuel volume displayed on the HMI during validation event \ensuremath{i}},
sort={VHMIi}
]%
{\ensuremath{V_{\text{HMI},i}}}

\newterm
[%
label={not:ei},
description={fuel-volume error for validation event \ensuremath{i}},
sort={ei}
]%
{\ensuremath{e_i}}

\newterm
[%
label={not:fi},
description={filtered fuel-level percentage at telemetry row \ensuremath{i}},
sort={fi}
]%
{\ensuremath{f_i}}

\newterm
[%
label={not:deltafi},
description={change in filtered fuel level between successive telemetry rows},
sort={deltafi}
]%
{\ensuremath{\Delta f_i}}

\newterm
[%
label={not:di},
description={distance travelled between telemetry rows \ensuremath{i-1} and \ensuremath{i}},
sort={di}
]%
{\ensuremath{d_i}}

\newterm
[%
label={not:vi},
description={vehicle speed at telemetry row \ensuremath{i}},
sort={vi}
]%
{\ensuremath{v_i}}

\newterm
[%
label={not:kmpli},
description={fuel-economy estimate for telemetry row \ensuremath{i}},
sort={kmpli}
]%
{\ensuremath{\mathrm{kmpl}_i}}

\newterm
[%
label={not:precision},
description={precision of the anomaly detector, computed as \ensuremath{TP/(TP+FP)}},
sort={precision}
]%
{\ensuremath{\mathrm{Precision}}}

\newterm
[%
label={not:fpr},
description={false-positive rate of the anomaly detector, computed as \ensuremath{FP/(FP+TN)}},
sort={FPR}
]%
{\ensuremath{\mathrm{FPR}}}

\newterm
[%
label={not:q0985},
description={0.985 quantile of clean training anomaly scores used as the decision threshold},
sort={q0985}
]%
{\ensuremath{q_{0.985}}}

\newterm
[%
label={not:Dtrain},
description={training partition used within a leave-one-trip-out fold},
sort={Dtrain}
]%
{\ensuremath{\mathcal{D}_{\text{train}}}}

\newterm
[%
label={not:Dtest},
description={held-out trip partition used for evaluation within a leave-one-trip-out fold},
sort={Dtest}
]%
{\ensuremath{\mathcal{D}_{\text{test}}}}
